# Regression test for the segfault: max_tree_size 1 makes libaca's edge table
# capacity 0, and aca_build() then divides by it. Zeek reaches this size for an
# empty pattern set, so a crash here is a crash of a running sensor.
# @TEST-EXEC:	paraglob-test -t 1 anything > out
# @TEST-EXEC:	btest-diff out
